/**
 * Services 统一导出
 *
 * 使用方式：
 *   import { floorService, uploadService } from '@/services';
 *
 *   const data = await floorService.getFloorList('mobile');
 */

export { floorService } from './floorService';
export { uploadService } from './uploadService';
